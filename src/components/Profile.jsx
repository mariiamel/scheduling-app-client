import Appointments from './Appointments.jsx'

const Profile = (props) => {
    return(
        <div>
            <h1>This is the Profile page</h1>
            <Appointments />
        </div>
    )
}

export default Profile